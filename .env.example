# Frontend -> API base URL. Use /api in Netlify so the configured proxy avoids browser CORS.
#   production : https://velorix-sentinel-backend.onrender.com/api
#   local API  : http://localhost:8081/api   (only when you deliberately run the backend locally)
VITE_API_BASE_URL=https://velorix-sentinel-backend.onrender.com/api
